## React virtual tree example

An example how to create a tree using `react-virtual`. The goal was to create a tree that does not have performance issues on expands/scrolls.
The `useSize` hook is used to check whether resizes/recalculations perform well with the tree (they do).
